import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  textCategory: {
    fontSize: 30,
    fontWeight: 'bold',
    marginLeft: 10,
  },
});
